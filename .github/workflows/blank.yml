name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Show Message
        run: echo "CI Running Successfully"

      - name: Show Files
        run: ls

      - name: Show Date
        run: date
